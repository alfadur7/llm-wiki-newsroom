# Directory Layout

The directory structure of this project, plus its location and prefix conventions.

## Layout

```
raw/          # Immutable source documents — never modify these
  _inbox.md   # Multi-channel URL queue (unified entry point for mobile/interactive/background, auto-managed)
              # Meta-line format: `URL  # source=... gap=... hub=...` (.claude/operations/gap-detection-rollout.md)
  _archive.md # Accumulated fetch_inbox.py run results (includes channel and outcome, auto-managed)
wiki/         # Claude owns this layer entirely · Obsidian vault root
  index.md              # Full page catalog + drill-down entry point (auto-generated)
  overview.md           # Layer 2-4 global view of the landscape axis (hybrid AUTO/EDITOR)
  contradiction.md      # Layer 2-4 global view of the conflict axis (hybrid AUTO/EDITOR)
  _backlinks.json       # Backlink index (auto-generated, for programmatic use)
  sources/              # Layer 2-1 source reflections (created on ingest)
    _source_map.json    # Ingest dedup-detection map (auto-generated)
    _catalog.md         # Full source catalog (auto-generated)
    _catalog-<cluster-slug>.md  # Per-cluster sub-catalog (auto-generated)
  entities/             # Layer 2-2: companies, institutions, people
  concepts/             # Layer 2-2: technology, strategy, regulatory concepts
  timelines/            # Layer 2-2: per-entity chronological timelines
  contradictions/       # Layer 2-3 conflict-axis theme-level analysis
    _contradictions.json          # Layer 2-2 program-layer raw issue DB (auto-generated)
    _contradictions_themes.json   # Layer 2-3 theme ↔ claim mapping SoT
    <theme-slug>.md               # Per-theme contradiction analysis (type: contradiction, editor-defined slug)
  overviews/            # Layer 2-3 landscape-axis cluster-level view
    <cluster-slug>.md   # Per-field view (type: overview, 1:1 mapping with cluster slug)
  syntheses/            # Layer 2-3 Q-A axis (stored query responses)
  trails/               # Layer 2-3 path axis (Memex associative trails)
graph/
  _graph.json           # Graph data (auto-generated, loaded by graph.html via fetch)
  _clusters.json        # Leiden-topology clusters + source assignments (auto-generated, fetched by graph.html)
  _overlays.json        # Meta-page overlay structure (auto-generated, fetched by graph.html)
  _dependencies.json    # Layer-cascade upstream index (auto-generated, used for staleness judgments)
  _pages.json           # Reading-panel body bundle (auto-generated, lazy-fetched on click in graph.html, .gitignore)
  _health-log.jsonl     # Cluster health-trend log (.gitignore)
  cluster_labels.json   # Cluster slug/name/anchor_members (human-edited)
  graph.html            # Sigma.js v3 visualization + body-reading panel (view via web server — fetch-based, file:// not supported)
wiki-export/            # Merged .md for Claude.ai Project Knowledge (auto-generated)
_site/                  # Hosting deploy staging — <slug>.html + <slug>-{graph,clusters,overlays,pages}.json (obscure filenames, .gitignore, generated by export and deployed to Cloudflare Pages)
tools/                  # Python scripts (no API key required)
  _feedback-review.json # Feedback-review cycle watermark + history (tool-managed, gitignored/operator-local; recorded by mine_feedback --checkpoint)
  _defect-log.jsonl     # Defect/transfer corpus — lint and Desk defects + guideline-fix accept/reject (tool-appended, gitignored/operator-local; input to the automatic mine_failures channel)
  _failure-review.json  # Failure-review cycle watermark + history (tool-managed, gitignored/operator-local; recorded by mine_failures --checkpoint)
  regression_set.json   # Held-out regression canary for guideline self-evolution — a fixed set of representative pages by type (human-edited)
tests/                  # tools/ and hooks regression tests (`python -m pytest tests/`)
.claude/                # Claude Code guidelines
  agents/               # The 5-role SoT
  commands/             # The 9-slash-command SoT
  layers/               # Per-content-Layer SoT (page format · authoring · rubric)
  skills/               # Content-agnostic craft modules (Agent Skills)
  policies/             # Global conventions (this folder)
  operations/           # One-time procedures · infra setup
  hooks/                # Automation (shell)
  memory/               # local memory (feedback/decision notes — distinct from the system auto-memory)
log.md                  # Chronological operations record (append-only · tracked)
lint-report.md          # Lint results (for user viewing · generated · untracked)
```

## File Naming & Location Rules

### Prefix Convention

| Type | Prefix | Examples |
|------|--------|----------|
| **Auto-generated data** (overwritten by scripts each run, do not edit) | `_` | `_backlinks.json`, `_source_map.json`, `_graph.json`, `_clusters.json`, `_catalog*.md`, `_contradictions.json` |
| **Human-edited config / viewable content** (Obsidian/browser access · manual editing) | none | `index.md`, `overview.md`, `contradiction.md`, `log.md`, `lint-report.md`, `graph.html`, `cluster_labels.json` |

Naming rule: under `wiki/` and `graph/`, a `_` prefix means an auto-generated data artifact (do not edit); no prefix means human-edited/viewable. Codified exceptions: `wiki/contradictions/_contradictions_themes.json` is a Claude-re-derived mapping SoT (edited by Claude, never by scripts), `raw/_inbox.md`·`raw/_archive.md` are append queues, and under `tools/` the `_` prefix marks internal modules and operator-local state instead.

`log.md` is append-only; `overview.md` and `contradiction.md` are hybrid (AUTO + EDITOR) mode.

### Location Rules

- Repo root: operational outputs (log, lint-report)
- `wiki/` root: wiki-wide scope meta (index, overview, contradictions, _backlinks)
- `wiki/sources/`: sources-only meta (source_map, catalogs)
- `wiki/contradictions/`: conflict-axis only (raw DB `_contradictions.json` + theme-level analysis .md)
- `wiki/overviews/`: landscape-axis cluster-level analysis (content only)
- `graph/`: graph-related outputs + cluster-label config (`_graph.json`, `_clusters.json`, `cluster_labels.json`, `graph.html`)
- `wiki-export/`: external-distribution outputs
- `tools/`: Python scripts (domain config lives in `graph/`). Exception — of the self-improvement data only the `regression_set.json` canary is committed (a fixed held-out set that ships with the distribution); the `_defect-log.jsonl` corpus and the `_failure-review.json`/`_feedback-review.json` watermarks are gitignored operator-local state (like `graph/_health-log.jsonl`), so a clone's accumulated self-evolution history — and any operator working notes in it — never enters the public mirror
- `tests/`: pytest regression tests — keeping the bug classes found in audits (`---`-in-value · escape corruption · judgment divergence · hook contract) as executable guards

## The Human-Edit Convention for `cluster_labels.json`

This is the definition file (`slug`, `name`, `anchor_members`) of the stable labels a human has assigned to auto-detected groups, and it is **edited directly by a human**. Scripts only read this file; they match the communities Leiden finds to labels by anchor-containment similarity.

When an unnamed group appears (the `[D]` output of `python tools/lint.py graph clusters`), a `{slug, name, anchor_members}` entry is added to this file after human-reviewer approval. **Claude must not edit it automatically** (naming is a domain judgment, so it requires human judgment).
